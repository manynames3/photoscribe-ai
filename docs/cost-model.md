# Cost Model

Phase 1 scope keeps costs limited to foundational AWS services:

- S3 photo bucket storage and infrequent object uploads.
- Two low-volume Lambda functions.
- HTTP API Gateway requests.
- CloudWatch logs and metrics.
- Billing alarm SNS notifications.

The Phase 2 and Phase 3 cost model will add Bedrock inference and S3 Vectors request/storage estimates.

